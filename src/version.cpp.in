/*
** This file is rebuilt and substitited every time you run a build.
** Things which need to be per-build should be defined here, declared
** in the version.h header, and then used wherever you want
*/
#include "version.h"

namespace Audioapp
{
   const char* Build::MajorVersionStr = "@AUDIOAPP_MAJOR_VERSION@";
   const int   Build::MajorVersionInt = @AUDIOAPP_MAJOR_VERSION@;
   
   const char* Build::SubVersionStr = "@AUDIOAPP_SUB_VERSION@";
   const int   Build::SubVersionInt = @AUDIOAPP_SUB_VERSION@;
   
   const char* Build::ReleaseNumberStr = "@AUDIOAPP_RELEASE_NUMBER@";
   const char* Build::ReleaseStr = "@AUDIOAPP_RELEASE_VERSION@";
   
   const char* Build::BuildNumberStr = "@AUDIOAPP_BUILD_HASH@"; // Build number to be sure that each result could identified.
   
   const char* Build::FullVersionStr = "@AUDIOAPP_FULL_VERSION@";
   const char* Build::BuildHost = "@AUDIOAPP_BUILD_FQDN@";
   const char* Build::BuildArch = "@AUDIOAPP_BUILD_ARCH@";

   const char* Build::BuildLocation = "@AUDIOAPP_BUILD_LOCATION@";

   const char* Build::BuildDate = "@AUDIOAPP_BUILD_DATE@";
   const char* Build::BuildTime = "@AUDIOAPP_BUILD_TIME@";

}

